# Minimal requirements for PythonAnywhere deployment
# Optimized for free tier disk space limits
# Heavy packages removed - charts rendered client-side with JavaScript

# Core Django
Django==5.2.5
sqlparse==0.5.3
asgiref==3.9.1
tzdata==2025.2

# Django REST Framework for API
djangorestframework==3.14.0

# CORS support for local network API calls
django-cors-headers==4.3.1

# Forms and UI
django-crispy-forms==2.4
crispy-bootstrap5==2025.6

# Environment variables
python-dotenv==1.0.1

# System monitoring (required for monitoring page)
psutil==7.0.0

# REMOVED - Not used in webplatform codebase:
# ❌ numpy (2.3.2) - not imported anywhere (~30MB saved)
# ❌ pandas (2.3.1) - not imported anywhere (~50MB saved)
# ❌ matplotlib (3.10.5) - not imported, charts use JavaScript (~50MB saved)
# ❌ seaborn (0.13.2) - not imported anywhere (~10MB saved)
# ❌ plotly (5.24.1) - not imported, charts use JavaScript (~30MB saved)
# ❌ requests (2.32.4) - only used in test scripts, not webplatform
# ❌ pytest (8.3.4) - only for development/testing
# ❌ PyYAML (6.0.2) - not imported anywhere
# ❌ python-dateutil - not directly imported

# Total savings: ~170MB - fits easily in PythonAnywhere free tier!

# Note: Charts are rendered client-side using Chart.js/JavaScript (via CDN)
# No Python visualization libraries needed on the server

# The following run on LOCAL network only (not on PythonAnywhere):
# - torch (800MB+) - model inference runs locally
# - transformers - LogBERT model runs locally
# - kafka-python - Kafka consumer runs locally
# - channels/redis - not needed for REST API architecture
# - fastapi/uvicorn - using Django REST Framework instead
